- for player in @lineups
  %button.btn{ href: '#',
    autocomplete: 'off',
    data: { action: @action, from: @id, to: player.id, loading-text: 'Submitting...' } }= "#{player.position}: #{player.name}"
