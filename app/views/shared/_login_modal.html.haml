/ Modal Partial
- unless user_signed_in?
  #loginModal.reveal-modal
    .row.span12.centered
      .row
        .span6
          %h5 Sign In
          = form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f|
            = f.label :email
            = f.email_field :email
            %br
            = f.label :password
            = f.password_field :password
            %br
            = f.submit "Sign in" , :class => "nice large radius blue button"

        .span6
          %h5 New User? Sign up!
          %p Creating an account only takes a few minutes. Get started now!
          %a{:href => "/payments/payment", :class => "nice large radius green button"} Create an Account!

    %a.close-reveal-modal
      X
/ Modal Partial End
