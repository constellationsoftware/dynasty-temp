= javascript_include_tag controller.action_name
= stylesheet_link_tag controller_base_name

- content_for :sub_nav do
  = render 'shared/sub_nav'
- content_for :title, 'Game Review'

.row
  %dl.tabs
    - @games.each_with_index do |game, i|
      - week = i + 1
      %dd.tab{ :id => "week_#{week}", 'data-game' => "#{game.id}" }
        %a{ :href => "#week_#{week}",
          :class => game_review_tab_class(game.won?(@team), @week === week || (@week.zero? && i.zero?)) }
          = t '.tab_label', :week => week
  %ul.tabs-content
    - @games.each_with_index do |game, i|
      - week = i + 1
      %li{ :id => "week_#{week}Tab",
        :class => game_review_tab_class(game.won?(@team), @week === week || (@week.zero? && i.zero?)) }
        -#- if game.scored?
        -#  .row
        -#    %h5.twelve.columns
        -#      = t ".#{game.won?(@team) ? 'victory' : 'defeat'}", :opponent => game.opponent_for(@team).name
        -#.row
        -#  %h6.five.columns
        -#    %strong= game.away_team.name
        -#  %h6.two.columns vs.
        -#  %h6.five.columns
        -#    %strong= game.home_team.name
