%table{ :id => 'balance_table', :width => 500 }
  %tr
    %td
      Rank
    %td
      Account
    %td
      Balance
  - @value_list.each_with_index do |item, index|
    %tr
      %td
        = index + 1
      %td
        = "Team ##{item['account']}"
      %td
        = "$#{item['sum(amount)']}"
