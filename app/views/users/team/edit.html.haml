= render :partial => 'shared/controls/registration_steps', :locals => { :step_classes => [ 'active', 'active', '' ] }
.row-fluid
  .span12.well.light-well
    %h2= t('.title')
    = simple_form_for @team do |f|
      = f.input :name
      = f.input :motto
      = f.button :submit, 'Submit'
